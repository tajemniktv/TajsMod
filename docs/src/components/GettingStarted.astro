---
const steps = [
  {
    step: 1,
    title: "Subscribe on Steam",
    description:
      "Head to the Steam Workshop and click Subscribe. The mod will download automatically.",
    icon: "🎮",
  },
  {
    step: 2,
    title: "Launch Upload Labs",
    description:
      "Start the game normally. The mod loads automatically with no extra steps needed.",
    icon: "🚀",
  },
  {
    step: 3,
    title: "Access Mod Settings",
    description:
      "Click the puzzle piece icon in-game to open the mod settings panel.",
    icon: "⚙️",
  },
  {
    step: 4,
    title: "Explore & Customize",
    description:
      "Use the Command Palette (MMB/Space) for quick access to all features.",
    icon: "✨",
  },
];
---

<section class="section bg-gray-50 dark:bg-dark-950">
  <div class="container-wide">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">Getting Started</h2>
      <p class="text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
        Up and running in under a minute
      </p>
    </div>

    <div class="max-w-4xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {
          steps.map((item, index) => (
            <div
              class="card flex items-start gap-4"
              style={`animation-delay: ${index * 0.1}s`}
            >
              <div class="flex-shrink-0 w-12 h-12 rounded-xl bg-accent-500/10 flex items-center justify-center text-2xl">
                {item.icon}
              </div>
              <div>
                <div class="flex items-center gap-2 mb-1">
                  <span class="text-xs font-bold text-accent-500 uppercase tracking-wider">
                    Step {item.step}
                  </span>
                </div>
                <h3 class="font-semibold text-lg mb-1">{item.title}</h3>
                <p class="text-sm text-gray-600 dark:text-gray-400">
                  {item.description}
                </p>
              </div>
            </div>
          ))
        }
      </div>

      <div
        class="flex flex-col sm:flex-row items-center justify-center gap-4 mt-10"
      >
        <a
          href="https://steamcommunity.com/sharedfiles/filedetails/?id=3628222709"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-primary"
        >
          Subscribe on Steam Workshop
        </a>
        <a href={`${import.meta.env.BASE_URL}/docs/`} class="btn-secondary">
          Read Full Documentation
        </a>
      </div>
    </div>
  </div>
</section>
